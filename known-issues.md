# 已知暂缓处理的问题（Known Issues）

记录代码审查中发现、评估后暂不修复的问题，供后续迭代参考。修复其中任何一项时请更新或删除对应条目。

来源：2026-08-22 对提交 `0f92dea`（bash.run / web.search 校验收紧 / 会话历史渲染修复）的代码审查。同次审查中已修复的问题见 `ChangeLog.md`；以下两项为有意搁置。

---

## 1. 会话历史时间戳的启发式配对错位

**严重度**：低（纯显示问题，不影响数据）

**现象**：重新打开历史会话时，助手消息旁悬停显示的时间戳可能错位——拿到上一次 run 的时间、显示同一分钟，或不显示时间。

**成因**：服务端没有记录"哪条助手消息由哪次 run 产生"。`session.messages` 与 `session.runs` 是两个平行列表，无显式关联。前端加载历史时按消息顺序做启发式配对（`public/js/workbench.js:2267-2281`）：

- 遇到用户消息就按序消耗 `runs` 数组的下一个 run，记为 `lastRun`；
- 紧随其后的助手消息取 `lastRun?.completedAt` 作为时间戳（`workbench.js:2271`）。

该"一条用户消息 → 一个 run → 下一条助手消息属于它"的假设在以下场景失效：

| 场景 | 结果 |
|------|------|
| 连续重复的用户消息被展示层去重（`workbench.js:2254-2257`），但 `runs` 每次发送都有一条 | 后续配对整体错位 |
| 某条用户消息发送失败、未产生 run | 下一条助手消息复用更早 run 的时间 |
| run 失败或被取消（`completedAt` 仅从 `run.completed` 事件提取，`lib/agent/trace.js:69-75`） | 对应助手消息无时间戳 |
| 一次 run 产生多条助手消息 | 全部显示同一分钟 |

**为什么暂不修**：只影响悬停显示的小时间戳，实时消息使用真实事件时间，任何数据都不会损坏。根治需要改持久化结构（见下），为一个显示瑕疵不值得在本次修。

**修复思路**：服务端在把助手消息写入 `session.messages` 时直接记录 `runId` 与 `completedAt`（或在 run 对象里记录产出消息的 id），前端改为查表而非顺序猜测。涉及会话持久化格式与会话历史 API，需同步考虑旧数据兼容（无关联字段的旧会话可回退到现有启发式）。

---

## 2. 大输出写入 agent-runs.json 的写放大

**严重度**：中低（性能与磁盘磨损，非正确性问题；单用户本地部署下影响有限）

**现象**：Agent 执行输出量大的命令（npm install、git log 等，bash.run / code.run 单次输出上限 1MB）后，run 期间磁盘写入量远超实际新增数据。

**成因**（三段逻辑叠加）：

1. 工具结果（最多 1MB 输出）被 JSON 序列化后存入 `run.messages`（`lib/agent/runtime.js:674`），且 `tool.completed` 事件的 payload 又携带完整 result 存入 `run.events`（`lib/agent/runtime.js:154`、`437`）——同一份输出持久化两份。
2. run 执行期间每个事件都调用 `emit()`（`lib/agent/runtime.js:163-169`）→ `store.saveRun(run)`。
3. `saveRun`（`lib/agent/store.js:141-149`）是"读整个 `agent-runs.json`（最多 200 个 run）→ 替换该 run → 整文件重写"。

即：run 里一旦嵌有 1MB 输出，之后**每发一个事件**都把含这 1MB 的整个文件重写一遍。例：文件已 5MB、一轮 30 个事件 → 约 150MB 磁盘写入。一轮多工具调用时事件数更多。

**现有缓解**：

- 单次工具输出硬上限 1MB（`lib/computer/code.js` 的 `MAX_OUTPUT`，`bash.js` 同步引用）；
- `agent-runs.json` 最多保留 200 个 run；
- 2026-08-22 修复了输出超限后的无限缓冲（超 1MB 即停止拼接并杀进程，结果带 `outputTruncated` 标记），单次调用内不再膨胀。

**为什么暂不修**：像样的修法都要动持久化格式或事件语义，牵连 SSE 重放、备份恢复（`lib/workspace/`）、会话历史 API，属于一次独立重构，不适合搭车小改。

**修复思路**（可选其一或组合）：

- **防抖合并写**：`saveRun` 攒批（如 200ms 窗口或事件数阈值），事件流结束/等待审批时强制落盘。改动最小，但要处理好进程退出前的 flush 与崩溃时的丢失窗口。
- **大输出外置**：超过阈值（如 64KB）的 `data.output` 存独立文件（`agent-run-files/`），`run.messages` / `run.events` 里只存引用与摘要。收益最大，但备份/恢复、历史 API、前端展示都要跟着改。
- **去重存储**：`tool.completed` 事件 payload 不再携带完整 result（重放时可从 `run.messages` 取）。只消掉双份存储，写放大仍在。

---

*最后更新：2026-08-22*
