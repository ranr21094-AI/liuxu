# 全量代码审查说明与修复计划（第二轮评审修订版）

> 第一轮审查日期：2026-08-22（16 项问题清单）
> 第二轮评审日期：2026-08-22（逐条对照代码复核，修正定级与方案）
> **修复完成日期：2026-08-22 —— 全部 15 项适用修复已实施并通过自动化测试（239/239）；Chrome 扩展链路待真机手工验证。详见文末"修复状态"与 ChangeLog.md。**
> 覆盖范围：服务端、工作台前端、Agent、电脑工具、Chrome 扩展、备份恢复、依赖与测试

## 威胁模型定位（本版修订的前提）

第二轮评审确认：第一轮报告的**代码论断基本属实**（重复路由、`web.fetch` 无地址校验且整包缓冲、runtime 权限标记被请求覆盖、审批缺省视为批准、依赖 CVE 版本号、测试环境变量遗漏等均已逐条核实）。问题不在事实错误，而在**威胁模型错位**：第一轮按多用户公网部署的标准评估，而本系统的实际边界是：

- 需要登录，当前为**单用户本地部署**（localhost / 内网单人使用），服务操作者即账户所有者；
- 多数"攻击路径"的前置条件是拿到账户登录态——拿到登录态后攻击者本就可以直接操作界面；
- 所有者已在既往决策中明确接受部分风险（自定义供应商放开内网地址、bash.run 任意命令 + 逐次确认、电脑工具默认开启）。

因此本版的处置原则：**保留真实且修复便宜的问题；将方案规模收敛到与单人本地部署相称；砍掉与企业边界绑定的重型机制**。与第一轮方案冲突处，以本版为准。

## 结论：四档处置

| 处置 | 数量 | 编号 |
| --- | --- | --- |
| 照单修复（真实且便宜） | 7 | CR-02、CR-07、CR-08、CR-12、CR-14、CR-15、CR-16 |
| 缩小范围（问题真实，方案减配） | 5 | CR-01、CR-03、CR-04、CR-06、CR-09 |
| 替代方案（不按原方案做） | 3 | CR-05、CR-10、CR-11 |
| 已过时（无需修复） | 1 | CR-13 |

## 问题总览（含裁决后定级）

| 编号 | 原级 | 裁决级 | 问题 | 裁决 |
| --- | --- | --- | --- | --- |
| CR-01 | P1 | P2 | `web.fetch` 整包缓冲后才查体积上限 | 只修流式限体积；**不做地址封禁**（与既定内网放开决策一致，且会挡住对本机服务的合法 fetch） |
| CR-02 | P1 | P1 | pdfjs-dist@3.11.174 在 CVE-2024-4367 受影响范围 | 照单修复：升级 + `isEvalSupported: false` |
| CR-03 | P1 | P2 | runtime 的私密权限标记被同账户后续请求覆盖 | 照单做"run 启动时快照权限"；"跨会话越权"定性夸大（同账户=同一人），不建 per-run 令牌校验机制 |
| CR-04 | P1 | P2 | Agent 附件直接读盘绕过私密图片路由检查 | 照单做双重检查；定级降为 P2（前置条件是所有者自己构造/复用附件） |
| CR-05 | P1 | 不采纳原方案 | 电脑工具缺省开启 + 默认放行用户主目录 | **保持默认开启**（唯一管理员即所有者，默认关损害使用）；替代方案：默认白名单从整个主目录收缩到工作区目录 |
| CR-06 | P1 | P2 | 超时后宽限期结束即释放运行槽，进程可能未死 | 保持现有权衡（宽限强结算优于僵尸槽锁死到重启）；至多补一次 killTree 重试；**不建** PID 追踪 + `termination_unconfirmed` 状态机 |
| CR-07 | P1 | P2 | 审批请求缺少 `approved` 字段时默认视为批准 | 照单修复（严格布尔，缺失/类型错误返回 400）；P1 定级夸大——利用前提是登录态 + approvalId，有这两者直接点批准效果相同 |
| CR-08 | P1 | P1 | 思考参数与 ZDR/reasoning 参数互斥分支，同时开启时 ZDR 静默丢失 | 照单修复：参数独立组装（真实的组合交互缺陷） |
| CR-09 | P1 | P2 | 供应商级能力无法表达模型差异（迁移后 DeepSeek 视觉模型失去视觉、Kimi 思考统一化） | 值得修，但属**功能修复**而非安全 P1：模型级覆盖 `模型 > 供应商默认` |
| CR-10 | P1 | 替代方案 | ZIP 恢复先写核心数据后处理附件，失败留半恢复状态 | 不做事务引擎（yauzl 流式 + 暂存目录 + 事务清单 + 原子切换 + 启动恢复）；替代：**恢复前预检全部 JSON + 自动留一份旧数据目录副本** |
| CR-11 | P2 | 替代方案 | Chrome 客户端工具无完整回传链路、端口写死 3000、无超时 | 修**功能缺口**（端口不限、真实转发、30 秒超时、结果回传）；**不做** ECDSA 配对/签名/防重放/版本协商——localhost 扩展对 localhost 服务做挑战应答加密，仪式感大于安全 |
| CR-12 | P2 | P2 | 拉取模型列表无法复用已保存密钥，刷新后须重输 | 照单修复：`providerId` + Base URL/格式严格匹配才复用加密密钥（方案本身设计得当） |
| CR-13 | P2 | 已过时 | 删除模型后旧默认模型无法清空 | `normalizeAiSettings` 现已将悬空模型自动重置为空（默认模型改为空的改造完成时生效），服务端自愈，无需修复 |
| CR-14 | P2 | P2 | multer@2.1.1 深层字段 DoS + 中止上传残留临时文件 | 照单修复：升级 2.2.0（利用前提是登录态，但升级便宜无害） |
| CR-15 | P2 | P2 | `POST /api/restore` 在 server.js 与 backup-routes.js 重复注册，旧路由遮蔽新处理器 | 照单修复：删除旧路由（已核实两处注册：server.js:2541、backup-routes.js:23） |
| CR-16 | P2 | P2 | 测试继承本机 `.env`（GETOKEN 相关变量未清理） | 照单修复：测试辅助函数清理全部 GETOKEN 变量（已核实清理列表确实遗漏） |

## 明细与裁决理由

### 照单修复（7 项）

**CR-02：PDF.js 恶意 PDF 脚本执行**
- 事实：`pdfjs-dist@3.11.174` 落在 CVE-2024-4367 受影响范围（≤4.1.392），特制 PDF 可利用动态代码求值执行脚本；官方缓解即 `isEvalSupported: false`。
- 修复：升级 `pdfjs-dist@4.2.67`，前后端所有 `getDocument` 显式 `isEvalSupported: false`，同步调整 ESM 入口与 Worker 构建。几十分钟工作量，无争议。

**CR-07：审批缺省视为批准**
- 事实：`approved: req.body?.approved !== false`，仅显式 `false` 拒绝；空 body/拼错字段/其他类型均视为批准。
- 修复：仅接受严格布尔；缺失或类型错误返回 400 且不改变 run 状态。注意：正式前端始终发送布尔值，此改动无兼容影响。

**CR-08：思考与 ZDR 互斥**
- 事实：参数组装为 if/else 链——命中 DeepSeek/Kimi 思考分支即跳过 OpenRouter 分支（ZDR、reasoning、web_search 工具）。供应商卡片同时开启思考与 ZDR 时 ZDR 静默丢失。
- 修复：思考参数与 OpenRouter 类参数独立组装，组合开启时同时发送；加请求快照测试覆盖组合。

**CR-12：拉取模型列表不能复用已保存密钥**
- 事实：供应商密钥加密存于服务端，公开接口只回 `apiKeyConfigured`；前端"拉取模型列表"只上送输入框明文，刷新页面后必须重输密钥。
- 修复：`POST /api/ai/custom-providers/models` 接受可选 `providerId`；仅当草稿 Base URL 与 API 格式与已保存供应商完全一致时复用加密密钥；任一改变要求新密钥；响应永不回明文。

**CR-14：Multer 公开漏洞**
- 事实：`multer@2.1.1` 受深层嵌套字段资源耗尽（GHSA-72gw-mp4g-v24j）与中止上传残留临时文件（GHSA-3p4h-7m6x-2hcm）影响；修复版本 2.2.0。
- 修复：升级 2.2.0。单人本地部署下利用前提（登录态）削弱实际风险，但升级零成本。同批评估 DOMPurify / Nodemailer 小版本升级，逐项确认无破坏后合入。

**CR-15：重复恢复路由**
- 事实：`server.js:2541` 与 `lib/http/backup-routes.js:23` 均注册 `POST /api/restore`；Express 按注册顺序，旧处理器先命中，新处理器中的缓存失效与迁移逻辑不执行。
- 修复：删除 server.js 中的旧路由，保留模块化处理器；加"路由仅注册一次"断言。

**CR-16：测试继承本机 `.env`**
- 事实：测试启动辅助函数清理了 DEEPSEEK/MOONSHOT/OPENROUTER/TAVILY/PERPLEXITY/SEEDREAM/WESTOCK/QQ 变量，但遗漏全部 GETOKEN 变量；本机配置可改变默认设置断言（已复现 1 例失败）。
- 修复：辅助函数按"影响应用默认值的变量全集"清理并在用例后恢复；加"本机 .env 不影响默认值"专门用例。

### 缩小范围（5 项）

**CR-01：`web.fetch` 整包缓冲 + 无地址校验**
- 事实（均核实）：不做任何地址校验（SSRF 工具函数仅用于生成图片 URL）；`redirect: 'follow'` 自动跟随跳转；`await response.arrayBuffer()` 先把整个响应读入内存再检查 `agentWebFetchMaxKb`，超大/无限响应可耗尽内存。
- 裁决：**只修缓冲问题**——改为流式读取、边读边累计、超限即中止（这是健壮性缺陷，与信任边界无关）。**不做地址封禁与逐跳校验**：所有者已明确接受内网直连姿势（自定义供应商放开内网），web.fetch 封禁 localhost/内网会直接挡掉对本机服务的合法抓取，且"攻击者诱导批准恶意网址"的前置条件是所有者自己批准。逐次确认门控维持现状。
- 原级 P1 → **P2**。

**CR-03：私密权限标记被同账户请求覆盖**
- 事实（核实）：`runtimeFor` 按 dataDir 缓存 runtime，`entry.hasDiaryAccessFlag` 被每个后续请求覆盖，运行中的 run 在工具调用时动态读取。
- 裁决：定级降为 **P2**——两个会话属于同一账户、同一所有者，"跨会话越权读取"预设了对立攻击者，实际是同一个人的两个标签页。照做便宜的部分：**run 启动时快照权限状态**，运行期不再受后续请求影响；不建 per-run 令牌绑定与逐调用验签。

**CR-04：Agent 附件读盘绕过私密图片路由保护**
- 事实：浏览器经受保护路由访问私密图片（检查日记权限），Agent 侧直接从磁盘读 `/uploads/...`。
- 裁决：定级降为 **P2**（触发前提是所有者自己复用带私密附件的旧运行或手工构造附件路径）；但"锁定内容流向云端供应商"确属隐私期望，照做**双重检查**（接收附件时 + 真正读盘发送前），任一步无有效授权即拒绝。

**CR-06：超时后进程可能未死而槽已释放**
- 事实：宽限期（5 秒）到即强制结算并释放运行槽，`taskkill` 失败时进程可能存活；Windows 上复现过临时目录被残留进程锁定。
- 裁决：**保持现有权衡**——对单人应用，槽被僵尸进程锁死到重启比偶发漏杀更伤（漏杀进程所有者可在任务管理器处理）。低成本补强：killTree 失败后补一次重试、宽限期略增；**不采纳** PID 持久追踪 + `termination_unconfirmed` 状态机 + 槽锁定至 PID 确认消失。原级 P1 → **P2**。

**CR-09：供应商级能力无法表达模型差异**
- 事实：迁移把能力压到供应商级——DeepSeek 卡 `supportsMedia: false` 使 `deepseek-v4-flash-vision-exp` 失去视觉；Kimi 三模型思考统一为 `optional`（k3 写死 max、k2.7 fixed 丢失）。
- 裁决：值得修（弥补统一化改造的实际功能降级），但属**功能修复**，定级 P2。方案照原样：模型条目扩展 `{ id, name?, supportsMedia?, thinking?, zdr? }`，优先级"模型覆盖 > 供应商默认"；旧字符串条目兼容读取；v2 迁移仅修正与 v1 自动生成内容完全一致的卡片，用户编辑过的不动，迁移前备份。

### 替代方案（3 项）

**CR-05：电脑工具缺省策略**
- 原方案（无策略文件时默认关闭 + 空白名单）**不采纳**：唯一管理员即所有者，默认关闭等于每次重装先去设置开一遍，与该功能的建设目的冲突。
- 采纳的事实部分：默认把**整个用户主目录**加入白名单确实过宽（agent 可自动读取家目录下任意非敏感标记文件）。
- 替代方案：保持 `computerToolsEnabled: true` 默认；默认白名单收缩为**工作区/数据目录**（去掉 `os.homedir()` 兜底，保留 `PREFERRED_ALLOWLIST` 中真实存在的目录）；已显式保存的策略不动。另注意电脑工具本就有 admin + 开关 + 写操作逐次确认三重门控，报告的"默认开放"表述未提这层。

**CR-10：ZIP 恢复完整性**
- 原方案（yauzl 流式解析、512MB/2 万条目/1GB 解压等全套限额、暂存目录、事务清单、目录原子切换、启动恢复未完成事务）**不采纳**——为个人笔记应用引入数据库级 WAL 机制，复杂度与风险收益不成比例。
- 替代方案（解决九成担忧的一成代码）：
  1. 恢复**写入任何数据前**先把 ZIP 内全部 JSON 解析校验一遍（含条目数与解压体积累计上限，防炸弹），校验失败不动线上数据；
  2. 开始写入前自动复制当前数据目录为一次性备份副本（成功后可清理），失败可手工回退；
  3. 拒绝绝对路径/盘符/`..`/符号链接逃逸；
  4. 存在活动 Agent run 时返回 409。
- "替换模式删旧残留"的语义改进列为可选后续，不阻塞。

**CR-11：Chrome 扩展桥接**
- 原方案（ECDSA P-256 配对、挑战签名、防重放、协议版本协商、同批发布）**不采纳**：localhost 页面上的扩展对 localhost 服务做公钥挑战应答，防的是"能往本机 localhost 注入脚本"的攻击者——而能做这件事的对手早已越过该防线；加密层只增加配对摩擦与双端耦合。
- 采纳的事实部分（功能性缺口，值得修）：前端收到 `client_tool.requested` 后只显示等待、未把任务转发给扩展；扩展写死 3000 端口；无超时导致 run 永久卡住。
- 替代方案：沿用现有共享密钥签名（已有 `chrome.js` 签名机制）；content script 来源判断改为主机名严格等于 `localhost`/`127.0.0.1` 且端口取自当前页面；补全转发与结果回传链路；**30 秒无结果自动失败恢复 run**；重复 `requestId` 返回首次结果。

### 已过时（1 项）

**CR-13：删除模型后旧默认模型残留**
- 原论断"保存出不存在的默认模型"在默认模型改为空（`DEFAULT_AI_SETTINGS.model = ''`）的改造后**不再成立**：`normalizeAiSettings` 对悬空引用（`isStoredAiModel` 判否）自动重置为空，服务端自愈。前端选择器为空时的 `|| current.model` 回退仅影响显示，不影响落库结果。无需修复；若日后想优化，仅需前端把空选择显式传空。

## 精简修复清单（按工作量排序）

| 批次 | 内容 | 预估 |
| --- | --- | --- |
| 快赢（每项 ≤ 半小时） | CR-07 审批严格布尔；CR-15 删重复路由；CR-16 测试环境变量清理；CR-01 web.fetch 流式限体积；CR-02 pdfjs 升级（含构建调整约 1 小时）；CR-14 multer 升级 | 合计约半天 |
| 中等 | CR-08 思考/ZDR 独立组装 + 快照测试；CR-12 拉取复用密钥（providerId 严格匹配）；CR-03 run 启动权限快照；CR-04 附件双重检查；CR-05 默认白名单收缩；CR-06 killTree 重试 + 宽限微调 | 各 0.5～1 天 |
| 较大（可独立排期） | CR-09 模型级能力覆盖（schema + UI + v2 迁移）；CR-10 恢复预检 + 自动备份；CR-11 Chrome 桥接功能补全（转发/端口/超时） | 各 1～3 天 |

每项修复要求：针对性测试 + 全量 `npm test` 通过 + 更新本表状态与 `ChangeLog.md`。

## 临时措施（精简版）

1. 不导入来源不明的 PDF（修完 CR-02 前有效）。
2. `web.fetch` 审批习惯照常：只批准认识的目标；避免明显异常的超大响应目标（修完 CR-01 前有效）。
3. 恢复前手工复制整个数据目录；恢复后核对附件与 Agent 会话（修完 CR-10 前有效）。
4. 同时启用思考与 ZDR 的组合暂不视为零数据保留已生效（修完 CR-08 前有效）。

（第一轮临时措施中"关闭电脑工具""不依赖 Chrome 扩展"等条目与系统既定使用方式冲突，已移除。）

## 安全公告与版本依据

- PDF.js CVE-2024-4367 / GHSA-wgrm-67xf-hhpq：受影响 ≤4.1.392，修复 4.2.67；官方缓解 `isEvalSupported: false`。<https://github.com/advisories/GHSA-wgrm-67xf-hhpq>
- Multer 深层字段 DoS / GHSA-72gw-mp4g-v24j；中止上传残留 / GHSA-3p4h-7m6x-2hcm：修复版本 2.2.0。
- 版本核对：<https://www.npmjs.com/package/pdfjs-dist>、<https://www.npmjs.com/package/multer>

## 依赖与测试基线（第一轮实测，供修复对照）

- 测试总数 232；本机运行曾出现 2 例环境相关失败：GETOKEN 环境变量改变默认设置断言（CR-16 已复现）、命令超时后临时目录被占用（CR-06 已复现）。
- `npm audit --omit=dev` 当时报 1 严重、5 高危、4 中等；直接可达且应优先处理的是 PDF.js 与 Multer，其余多为传递依赖，升级前逐项记录可达路径后决定。

### v1.0.0 发布前复核（2026-08-25）

- 正式桌面构建改为 `npm ci --ignore-scripts --omit=optional`，不再安装或打包 PDF.js 未使用的服务端 Canvas 原生模块；`canvas → @mapbox/node-pre-gyp → tar` 链路及其严重告警已从交付包移除。PDF 文字提取、浏览器预览和 251 项测试保持通过。
- `npm audit --omit=dev --omit=optional` 当前结果为：0 严重、1 高危、4 中等，共 5 个依赖节点；扫描时这些节点均为 `fixAvailable: false`。剩余节点为 Nodemailer、DOMPurify、Express / body-parser / qs，继续跟踪上游修复版本。
- 当前可达性收敛：桌面服务只监听 `127.0.0.1`；DOMPurify 以字符串结果模式清洗 Markdown，不使用公告所列的 `IN_PLACE`、`RETURN_DOM` 或自定义元素配置；邮件提醒的信封、传输名称、`raw` / `jsonTransport` 和 OAuth2 参数不接受外部请求直接控制。
- 该结果不是“零风险”声明。发布后若上游出现兼容修复版本，应在独立依赖升级中更新锁文件、重新生成 vendor 资源，并重复 PDF、Markdown、邮件提醒和桌面打包验收。

## 已确认的默认决策（本版）

- 照单修复 7 项、缩小范围 5 项、替代方案 3 项、过时关闭 1 项；不删除或隐藏相关功能来代替修复。
- 保持既定姿势：内网直连可用、电脑工具默认开启（白名单收缩）、bash.run 任意命令 + 逐次确认。
- 不引入：web.fetch 地址封禁、审批以外的新认证机制、恢复事务引擎、Chrome 桥接加密配对、命令终止 PID 状态机。
- 仓库中未跟踪目录 `.zcode/` 不属于本计划，不读取、不修改、不提交。

## 修复状态（2026-08-22 实施完成）

| 编号 | 状态 | 说明 |
| --- | --- | --- |
| CR-01 | ✅ 已修复 | `web.fetch` 改流式读取、超限即中止（`server.js` agentWebFetch） |
| CR-02 | ✅ 已修复 | pdfjs-dist 4.2.67 + 前后端 `isEvalSupported: false`，前端 ESM 动态导入，构建脚本与断言同步更新 |
| CR-03 | ✅ 已修复 | run 启动快照 `diaryUnlocked`，知识工具经 AsyncLocalStorage 按 run 上下文过滤，委托子 run 继承父快照 |
| CR-04 | ✅ 已修复 | 附件接收过滤（normalizeAgentAttachments）+ 发送前读盘过滤（provider-messages / seedream 参考图），均按当前解锁状态 |
| CR-05 | ✅ 已修复（替代方案） | 默认白名单仅首选工作区目录；已保存策略不变 |
| CR-06 | ✅ 已修复（轻量版） | taskkill 失败重试一次；KILL_GRACE_MS 5s→8s |
| CR-07 | ✅ 已修复 | `approved` 非严格布尔一律 400 |
| CR-08 | ✅ 已修复 | 思考参数与 OpenRouter 类参数独立组装（`const openrouterStyle`），组合开启同时发送 |
| CR-09 | ✅ 已修复 | 模型级 `supportsMedia/thinking/zdr` 覆盖（模型 > 供应商），`none` 显式关闭思考；v2 迁移 + 备份 + UI 行内覆盖编辑 |
| CR-10 | ✅ 已修复（替代方案） | 恢复前全量 JSON/限额预检 + 数据目录自动备份（`<dataDir>.restore-bak`）+ 活动 run 409 |
| CR-11 | ✅ 已修复（替代方案） | 前端经扩展 ID 转发（设置 → 电脑），扩展主机名严格匹配任意端口、browser.*→CDP 映射、自动附加标签页；服务端 30s 超时 + requestId 防重；**待真机验证** |
| CR-12 | ✅ 已修复 | `/models` 与 `/test` 支持 `providerId` 密钥复用（端点+格式严格匹配），测试用本地 mock 上游验证 |
| CR-13 | ➖ 无需修复 | 服务端规范化已自愈 |
| CR-14 | ✅ 已修复 | multer 2.2.0 + 上传限额收紧（fields/fieldNestingDepth） |
| CR-15 | ✅ 已修复 | 删除 server.js 重复路由；发现并补上 ctx 缺失的 `restoreRequiresDiaryAccess`（此前 500 的根因） |
| CR-16 | ✅ 已修复 | 测试辅助函数清理全部 GETOKEN 变量并支持注入 |

**验证**：`npm test` 239/239 通过（新增 6 个针对性用例：能力覆盖优先级、v2 迁移与幂等、日记快照、客户端工具防重、路由唯一性与审批严格布尔、密钥复用正反例）。

---

*本版为第二轮评审后的修订版，取代第一轮生成的原始方案（原始文件未单独存档，其事实描述已并入上文各条目）。*
