# 单行紧凑导航验收

日期：2026-09-08。工作区版本保持 1.2.11，不打包、不发布。使用独立临时数据目录和空会话验证，未读取真实笔记或模型密钥。

## 布局

品牌栏 48px，四个等宽文字标签 36px，下方间距 12px，合计 96px。此前布局含 1px 分隔线约 245px，本次约释放 149px（方案估算为 148px）给列表。桌面和抽屉内侧栏均为 288px，四标签实测各约 62.75px。

品牌不再是可点击控件；删除旧导航折叠偏好读取及隐藏样式，保留整条侧栏的收起功能。Memory 角标独立定位，显示上限为 `9+`，完整数量通过按钮无障碍名称和悬停提示提供。

## 自动验证

- `node --check public/js/workbench.js`、`git diff --check` 通过。
- 导航/工作区布局测试 6 项通过，覆盖静态品牌、四入口顺序、单行等宽、焦点样式、触控 44px 规则、旧折叠逻辑移除、独立侧栏控制。
- 执行实际 Memory 角标更新函数，验证 0、1、9、10、125 的可见性、`9+` 上限和完整无障碍名称。
- 全量 `npm test`：356 项，355 通过，0 失败，1 跳过；跳过项为 Windows 路径大小写测试，当前宿主是 macOS。
- 既有综合工作区测试同步移除旧纵向/可折叠导航断言；用户已有的后端与知识库测试修复保持不变。

## 实际浏览器检查

- Agent、知识库、Memory、待办切换后路由及 `aria-current` 正确。
- 桌面侧栏收起/展开正常；Tab 焦点轮廓可见。
- 520px 下使用 Enter 切到知识库，抽屉自动关闭，焦点返回“打开侧栏”；返回 Agent 正常。
- 三种尺寸、深浅主题的四标签均单行、不遮挡、不溢出；页面宽度检查无横向溢出。
- 下表保留全部改前/改后截图。主区域为既有空会话欢迎页；深色桌面中的圆形效果为现有鼠标聚光效果，不属于本次修改。

| 视口 / 主题 | 改前 | 改后 |
| --- | --- | --- |
| 1440 × 900 / 浅色 | [截图](before-1440-light.png) | [截图](after-1440-light.png) |
| 1440 × 900 / 深色 | [截图](before-1440-dark.png) | [截图](after-1440-dark.png) |
| 840 × 900 / 浅色抽屉 | [截图](before-840-light.png) | [截图](after-840-light.png) |
| 840 × 900 / 深色抽屉 | [截图](before-840-dark.png) | [截图](after-840-dark.png) |
| 520 × 800 / 浅色抽屉 | [截图](before-520-light.png) | [截图](after-520-light.png) |
| 520 × 800 / 深色抽屉 | [截图](before-520-dark.png) | [截图](after-520-dark.png) |

## 验证边界

触控 44px 规则经过代码/测试校验，未用真实触屏设备实测。Memory 高数量角标经过 DOM 测试，截图中的隔离工作区没有待确认记忆。未重新打包或测试安装版，也未修改各页面内部布局、路由恢复或助手状态逻辑。
