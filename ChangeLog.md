# 更新日志

## 2026-08-18

### Agent 工作台与知识库

- 新增知识文档适配层：现有日志继续作为 `log:<id>` 来源，新建笔记和导入文件写入 `knowledge-documents.json`。
- 新增 MiniSearch 本地混合检索、Markdown/TXT/PDF/DOCX 导入、认证读取知识原文件，以及 ZIP 工作区备份/恢复。
- 新增独立 Agent Runtime、工具审批、SSE 事件流和 L2/L3 记忆建议。
- 电脑工具仅管理员、环回访问且 15 分钟内重新验证密码后可用；Chrome 通过仓库内 Manifest V3 扩展附加日常标签页。
- 默认首页重构为 Agent / 知识库双栏工作台：左侧分别管理会话和统一知识文档，右侧专注对话、笔记编辑或文件阅读，任务仅作为 Agent 工具保留。
- Agent 会话接口拆分为摘要列表与按需详情，并支持重命名和软归档；运行断线重连、审批、引用跳转与记忆建议继续保留。
- 知识编辑新增 800ms 自动保存、版本冲突保护、Markdown 预览，以及导入文件的只读正文和关联笔记。
- 知识库侧栏改为“知识库 → 文件夹 → 文档”树：原一级分类作为知识库、子分类作为文件夹，笔记/旧日志/导入文件统一混排；新增 `/api/knowledge/tree`，移除前端来源与文件格式筛选，旧日志日期编辑仍回写 `logs.json` 且保留工时和置顶字段。
- 旧日志、任务、照片墙、模型设置与备份界面迁至 `/legacy.html`，从新首页设置中进入；新首页不再加载旧 DOM、样式和业务脚本。

## 2026-08-10

### 隐藏日记与暗语解锁

- 日记分类改为始终隐藏，移除了解锁/锁定按钮、密码弹窗和「个人设置」中的日记密码区域；解锁不再依赖账号级密码。
- 在搜索框输入「如意如意」即解锁并自动进入日记列表，再次输入同一暗语则重新锁定；解锁会话按账户隔离。
- 备份/恢复、AI 上下文、分类与上传等涉及日记数据的操作统一受暗语会话保护；`DIARY_PASSWORD_HASH` 环境变量已移除。

## 2026-07-26

### AI 日志分级检索

- 独立 AI 对话的日志访问改为“元数据筛选 → 本地关键词补召回 → 候选正文分析”，默认不再把所有允许日志的完整正文直接发送给模型。
- 筛选模型只接收日志 ID、标题、日期、分类、工时、正文长度和本地链接；服务端会丢弃幻觉 ID、越权 ID，并在本地用最多 8 个具体检索词扫描标题和正文。
- 最终上下文会明确区分仅含元数据和已包含完整正文的日志；用户明确要求全部日志时仍支持权限范围内的全量正文读取。
- 8/32 批限制改为按最终正文候选计算；大批次确认会展示目录、相关日志和正文候选统计，确认重试仍按最新账户权限重新校验。
- 前端新增筛选、准备读取和分批分析状态；任一中间阶段失败、超时或取消时不会返回部分答案、保存残缺消息或执行日志修改。

## 2026-07-23

### 日志 AI 浮窗与对话模型

- 日志内 AI 从编辑器右侧分栏改为桌面浮窗：拖动标题栏即可移动，位置会保存在当前浏览器；双击标题栏可恢复默认位置，移动端继续使用底部弹层。
- 输入框新增与独立 AI 对话共用的可搜索模型选择器和 OpenRouter 刷新目录；模型按日志内对话独立保存，新对话继承账户默认模型。
- 日志 AI 请求、生图 Prompt 优化和附件能力校验统一使用当前对话模型；切换模型后保留可见上下文，不兼容的图片或视频会在发送前明确提示。
- 消息区移除狭窄头像列，实际回答模型改为紧凑标签；输入框压缩为自动增高的双层操作区，桌面和移动端按钮布局更稳定。

## 2026-07-21

### AI 推理过程显示

- AI 对话可在消息内查看供应商实际返回的思考/推理内容；流式生成时实时展开并显示字符数，回答完成后默认收起。
- 推理区域支持键盘展开、深浅色主题和移动端触控；不返回推理内容的模型不会显示空区域，日志批次的中间推理仍不进入最终对话。

### AI 多对话并行

- 将全局发送锁改为按对话隔离：一个对话正在推理或生成时，可以切换到其他对话继续发送请求。
- 历史列表显示后台生成状态，完成和失败提示会标明所属对话；生成中的同一对话仍会阻止重复发送和删除。
- AI 历史写入改为串行队列，并在持久化时排除未完成的临时流消息，避免多个回答同时完成时相互覆盖或保存残缺内容。

### OpenRouter 模型目录刷新

- 模型选择器新增“刷新”按钮，可显式绕过 10 分钟新鲜缓存并从 OpenRouter 重新拉取当前账户可用模型。
- 刷新期间显示加载状态并阻止重复点击；目录摘要展示 OpenRouter 模型数量、缓存来源和最近更新时间。
- OpenRouter 暂时不可用时继续使用 24 小时内的旧目录并给出提示；没有可用旧目录时保留原有明确错误。

## 2026-07-20

### OpenRouter 通用多模型

- **账户模型目录**：新增固定地址的 OpenRouter 适配器，通过当前账户 `/models/user` 动态发现可用文本模型；目录按 Key 指纹缓存 10 分钟，上游暂时失败时可使用 24 小时内的旧目录。
- **可搜索模型选择器**：输入框与默认设置共用模型搜索器，展示名称、完整 ID、直连/OpenRouter 来源、上下文、输入模态和每百万 Token 价格；任意 `author/model` 仍须经服务端账户目录确认。
- **对话级模型**：新对话继承账户默认模型，切换只影响当前对话并保留可见历史；助手消息记录实际供应商与模型，隐藏推理只在同一 OpenRouter 模型的后续请求中续传。
- **统一推理与流式规则**：支持模型默认、关闭和模型声明的推理档位，保存 `reasoning_details`；OpenRouter 流必须收到 `[DONE]`，SSE 流内错误会移除残缺回答且不生成工具卡。
- **OpenRouter 联网**：联网时使用 `openrouter:web_search` Beta 工具，解析 `url_citation` 为来源卡片；不预调用或失败回退 Tavily/Perplexity，基础/高级搜索最多 5/10 个结果。
- **ZDR 默认开启**：账户设置新增 OpenRouter 零数据保留开关，默认通过 `provider.zdr=true` 限制路由，并明确提示可能减少可用供应商。
- **动态多模态**：依据目录能力启用图片和视频；私有本地副本转换为 Data URL，限制单图 10MB、单视频 25MB、每条消息最多 4 个且合计 25MB。

### AI 密钥保护

- **加密迁移**：DeepSeek、Moonshot、OpenRouter、Tavily、Perplexity 和 Seedream 账户 Key 统一使用 AES-256-GCM 加密，旧明文设置在首次读取时原子迁移。
- **独立主密钥**：Windows 默认保存在 `%LOCALAPPDATA%\work-log\ai-secrets.key`，可用 `AI_SECRETS_KEY_FILE` 覆盖；主密钥丢失、错误或密文认证失败时拒绝启动，不会生成新密钥覆盖。
- **备份边界**：账户 Key 与主密钥均不进入 JSON 备份；README 增加跨机器迁移时单独安全复制主密钥的说明。

### 测试

- 新增动态目录与缓存、价格/模态标准化、ZDR、联网引用、同模型推理续传、Base64 图片、严格流式结束、流内错误脱敏及密钥迁移失败关闭测试。

## 2026-07-18

### AI 对话体验

- **输入框切换模型**：AI 对话输入框新增模型切换，可直接选择 DeepSeek Flash、DeepSeek Pro、Kimi K3、Kimi K2.7 Code 与 Kimi K2.6；选择按对话保存并应用于下一条消息，失败时自动恢复原模型。
- **响应式操作栏**：桌面端保持单行操作，移动端将模型/联网与附件、技能、生图、发送按钮分行显示，避免窄屏挤压。
- **附件缩略图**：用户提问中的图片改为紧凑缩略图，单图不再预留空白列，多图自动换行；双击查看原图和视频播放逻辑保持不变。
- **剪贴板图片**：在 AI 对话输入框直接粘贴截图或复制的图片即可上传为附件；普通文字粘贴不受影响，并继续遵守现有格式、数量和大小限制。

### Kimi 模型与推理

- **三款 Kimi 模型**：独立对话、日志内 AI 和生图 Prompt 优化统一支持 `kimi-k3`、`kimi-k2.7-code`、`kimi-k2.6`，原 DeepSeek 设置与历史保持兼容。
- **精确推理参数**：K3 固定 `reasoning_effort=max`；K2.7 Code 不发送 `thinking`；K2.6 默认开启并保留思考，也可关闭；不向 Kimi 发送温度、`top_p`、`n` 或惩罚参数。
- **隐藏思考续传**：流式和非流式回答都可保存不渲染的思考内容，后续 Kimi 请求按模型要求原样续传；切换 DeepSeek 后自动剥离。
- **严格流式完成**：Kimi 流必须收到 `[DONE]` 才视为成功，提前断线会丢弃残缺回答，不保存历史或执行工具卡。

### 实验性 Kimi 官方联网

- **Formula 搜索**：新增“Kimi 官方联网（实验）”，使用 `moonshot/web-search:latest` 的工具发现、Fiber 执行和工具结果回传；工具定义按 Key 指纹缓存 10 分钟。
- **受控工具循环**：K3 首轮强制搜索，K2.7 Code 与思考中的 K2.6 使用自动选择；最多 4 轮、6 次搜索，严格校验工具名、参数和 `tool_call_id`。
- **不自动降级**：Kimi Formula 开启时跳过 Tavily/Perplexity；发现、执行或续传失败会明确报错，不返回部分结果，也不伪造来源卡片。
- **隐藏供应商轨迹**：加密搜索结果和中间工具消息仅作为隐藏续传数据保存，不展示、不复制、不参与全文搜索或备份导出。

### 图片与视频理解

- **双入口附件**：独立 AI 对话和日志内 AI 都新增回形针，可发送纯附件消息；原图片图标继续只表示 Seedream 生图。
- **账户级媒体存储**：新增 `ai-media/` 与原子写入的 `ai-media.json`，媒体和 Moonshot 文件映射完全按账户隔离。
- **格式与配额保护**：校验扩展名、MIME 和文件签名；单文件 100MB、每条消息最多 4 个且合计 100MB、每账户最多 1000 个和 10GB。
- **本地预览与远端复用**：图片支持双击放大，视频支持 Range 播放；Moonshot Key 变化或远端文件失效时从本地副本重传。
- **引用清理**：删除最后一个会话引用后清理本地文件并尽力删除远端文件，未发送超过 24 小时的附件会自动回收。

### 测试与文档

- 新增 Kimi 精确参数、隐藏思考、严格 `[DONE]`、Formula 缓存/ID 对齐、媒体签名、Range 和会话引用测试。
- README 与 `.env.example` 新增 Moonshot 配置、模型差异、多模态限制和实验联网风险说明。

## 2026-07-15

### 独立登录与多账户

- **独立 `/login` 页面**：未登录访问首页会由服务端跳转到 `/login`；登录成功后返回原始同源地址，已登录用户再次访问登录页会回到应用。
- **Cookie 会话**：移除首页登录弹层、前端 `sessionStorage` 明文密码和 Bearer Token；改用有效期 24 小时的 HttpOnly、SameSite=Strict Cookie，会话可跨服务重启恢复。
- **管理员创建账户**：新增管理员和普通成员角色。管理员可创建、重命名、启用、停用、调整角色和重置临时密码，但不能查看或进入成员工作区。
- **首次强制改密**：新账户和管理员重置后的账户首次登录必须修改临时密码；普通密码要求 10–128 个字符。
- **账户保护**：禁止停用或降级最后一个有效管理员；修改密码、重置密码或停用账户会立即撤销该账户的现有会话。
- **登录限速**：每个来源 IP 在 15 分钟内最多尝试登录 5 次，错误信息不区分用户名或密码错误。

### 数据与隐私隔离

- **按账户数据库实例**：日志、分类、待办、倒数日、照片墙、上传、AI 设置与历史、提醒设置、日记锁和备份恢复全部绑定当前登录账户。
- **独立存储目录**：首次管理员继续使用现有 `data/` 数据；新账户使用 `data/accounts/<账户 UUID>/`，相同记录 ID 或上传文件名不会跨账户串读。
- **上传访问隔离**：`/uploads/:filename` 只在当前账户的上传目录解析，未登录或其它账户无法读取文件。
- **AI 配置隔离**：每个账户单独保存 AI Key、访问策略和对话历史；服务器环境中的默认 AI Key 只作为旧管理员工作区的兼容回退，不自动共享给新成员。
- **日记密码隔离**：日记密码改为账户级配置；新成员默认不启用，解锁 Cookie 同时校验账户 ID，不能在账户之间复用。
- **提醒多账户调度**：待办邮件提醒按所有有效账户分别检查和发送，SMTP 服务器凭据继续由服务端统一配置。
- **账户级备份**：备份与恢复只处理当前账户数据，不包含用户注册表、密码哈希或会话；旧备份继续兼容。

### 迁移与故障保护

- **自动迁移管理员**：首次启动会将现有根目录数据原地归属 `admin`，使用当时的 `AUTH_TOKEN` 作为一次性密码，并继承 `DIARY_PASSWORD_HASH`。
- **旧认证停用**：创建 `users.json` 后不再接受 Bearer `AUTH_TOKEN`；后续修改环境变量也不会覆盖账户密码或日记密码。
- **原子写入与损坏保护**：用户注册表和会话文件采用原子写入；损坏时保存 `.corrupt-*.bak` 恢复副本并拒绝登录，不会回退成无密码访问。
- **启动前完整性检查**：首次创建管理员前会检查现有 JSON 数据；检查失败时不创建用户注册表，也不修改损坏的源文件。

### 账户界面

- **更多工具账户区**：新增当前账户、个人设置、账户管理和退出登录入口，支持深浅色主题、键盘操作和移动端布局。
- **个人设置**：成员可修改显示名称、登录密码以及启用、修改或关闭自己的日记密码。
- **账户管理页**：管理员可查看账户角色、状态、创建时间、最近登录时间和首次改密状态；界面不显示成员数据量、内容或工作区入口。

### 日志、待办与图片

- **倒数日模式**：待办页新增独立倒数日卡片网格，支持一次性日期、每年重复、搜索、统计、闰年处理以及香港业务日期计算。
- **日志置顶**：日志卡片右上角改为图钉按钮；选择分类时，置顶日志会在分页前优先排列，多条置顶按最近置顶时间排序。
- **图片双击预览**：日志卡片、编辑器详情预览和 AI 生成图片支持双击放大，可通过遮罩、关闭按钮或 `Escape` 退出。
- **AI 历史持久化修复**：AI 历史继续保存到当前账户的 `ai-chats.json`，刷新、服务重启和账户切换不会再共享或覆盖其它账户的历史。

### 测试与上线

- **隔离回归测试**：使用两个拥有相同记录 ID 和相同上传文件名的账户，覆盖日志、待办、倒数日、分类、AI、提醒、照片墙、上传、备份恢复和日记锁隔离。
- **认证回归测试**：覆盖登录、限速、Cookie 属性、24 小时过期、会话恢复、强制改密、账户管理、最后管理员保护、损坏文件和旧 Bearer 失效。
- **浏览器验证**：通过真实浏览器验证原地址返回、首次改密、账户管理弹窗、键盘关闭和 390px 移动端“更多工具”入口。
- **完整验证**：`npm test` 共 89 项测试通过，编辑器构建、Node 语法检查和 `git diff --check` 均通过。

---

## 2026-06-23

### 待办侧栏与日历
- **待办模式侧栏重组**：侧栏菜单文案改为“待办事项”；进入待办页时，左侧侧栏只保留日历和邮件提醒设置，主内容继续承载完整待办列表与编辑表单。
- **待办日期标记**：待办模式下的侧栏日历会特殊标记有截止日期的待办；点击日期只更新日历焦点，不会筛选或跳转待办列表。
- **邮件提醒入口迁移**：邮件提醒设置从待办页右侧编辑表单移到左侧待办侧栏，启用状态、收件邮箱、提醒时间和保存接口保持不变。
- **邮件提醒布局优化**：待办侧栏邮件提醒中的收件邮箱和提醒时间改为上下两行，避免侧栏宽度较窄时横向挤压。
- **待办统计收紧**：待办、今日、逾期、已完成统计改为标题行右侧的小卡片，减少页面顶部纵向占用。
- **分类添加弹窗**：筛选栏里的行内新增分类输入框改为 SVG 图标按钮；点击后通过弹窗输入新分类名称。
- **分类删除图标优化**：可删除分类的删除入口改为 pill 内部的垃圾桶 SVG 按钮，不再使用小圆 `×`。
- **重复待办**：待办编辑表单新增“不重复、每日、每周、每月、每年”选项；完成重复待办时保留当前记录，并自动生成下一期未完成待办。
- **重复标记**：待办列表会显示紧凑的重复规则标记，日历和邮件提醒继续基于实际生成的下一期待办工作。
- **邮件提醒覆盖所有分类**：每日提醒快照现在明确汇总所有待办分类中当天到期且未完成的任务，邮件正文会在任务标题前显示分类前缀，仍不展示优先级。

### 日志页
- **统计卡片移除**：日志页不再展示标题行统计卡片，也不再保留旧侧栏统计面板；统计接口仍用于刷新日志日历中的“有日志日期”标记。
- **正文式全屏编辑**：日志全屏改为只保留标题和正文编辑区，隐藏元信息、工具栏、预览切换、大纲和日志内 AI；退出后恢复进入前的编辑/预览状态。
- **日志内 AI 状态清理**：移除输入框下方重复的“正在思考”状态，只保留消息区中的回答中提示。

### 侧栏
- **薄荷绿清新化**：全局左侧栏变量改为更浅的薄荷绿，默认、待办、分类、AI 和工具模式侧栏统一继承新色调。

### 照片墙
- **新增照片墙模式**：侧栏菜单新增“照片墙”，进入后显示单一无界画布和照片墙专用工具侧栏。
- **主画布全幅化**：照片墙主区域移除“照片墙 / 无界画布”标题区，缩放百分比移到左侧侧栏，右侧空间全部交给画布。
- **图片画布交互**：图片复用现有上传接口，可在画布中拖动、等比缩放；画布支持平移、滚轮缩放、适应全部和重置视图。
- **文字评论与持久化**：每张图片下方可编辑一段评论，布局、尺寸、评论和层级保存到 `photo-wall.json`，并纳入备份/恢复。
- **安全删除策略**：删除照片墙图片只移除节点，不删除 `/uploads/` 原文件，避免影响日志或 AI 生图中复用的图片。

### 分类管理
- **直达二级日志浏览**：点击一级分类后直接进入二级分类侧栏和右侧日志列表；有二级分类时自动打开第一个，没有时显示新增二级分类空状态。
- **一级分类操作入侧栏**：一级分类重命名和删除移动到左侧一级分类选中项内，受保护分类继续隐藏不可用操作。
- **二级分类管理整合**：新增二级分类、日历显示开关、二级分类重命名、删除和拖拽排序统一放进新的二级分类浏览页面。
- **图谱入口移除**：分类管理页不再默认展示二级分类列表/图谱中间页，工作流改为更直接的列表式浏览。
- **Codex 工作台风格**：分类管理页改为白底、细灰边、低阴影的紧凑工作台样式，一级分类、二级分类和日志浏览统一为黑白灰控件。
- **一级/二级分类命名**：分类管理页将父分类/子分类文案调整为一级分类/二级分类，一级分类总数移到侧栏标题右侧，侧栏列表不再展示计数。
- **分类图标按钮**：新增、确认、取消、重命名和删除等分类操作改为 SVG 图标按钮，保留原有元素 ID、无障碍名称和点击逻辑。

### 文档与测试
- **README 同步待办侧栏**：更新待办入口、邮件提醒位置、日历标记、分类弹窗和统计面板状态说明。
- **回归测试更新**：同步待办侧栏、待办日历标记、邮件提醒迁移、分类添加弹窗、删除图标、重复待办和统计卡片删除的 DOM/CSS/API 断言。

---

## 2026-06-22

### AI 对话页
- **聊天工作台清新化**：独立 AI 对话页收成更安静的白色工作面，历史栏、消息卡、来源链接和输入器统一为浅边框、低阴影的轻量样式。
- **Codex 工作台风格**：独立 AI 对话页进一步改为 Codex 式白底工作台，历史侧栏收成紧凑列表，助手回答转为文档式阅读块，底部输入器改成命令框质感。
- **AI 细节黑白灰化**：历史侧栏移除“AI 工作台”和消息次数统计，设置、Tavily、生图、复制和重命名等按钮统一为黑白灰。
- **历史搜索与头像统一**：用户头像改为黑白 SVG，历史记录新增标题/全文搜索切换，并将当前选中态调轻。
- **精选每日诗词**：新对话空白页改为按日期轮换精选名句池，扩展池仍保留 1000 条公版古典诗词；AI 回答取消头像，用户头像保留。
- **消息操作区与历史分组**：用户消息也取消头像；问题悬浮时显示时间和复制按钮，AI 回答底部左侧显示复制、右侧显示时间；历史记录按今天、一周内和更久以前分组，不再显示单条时间。
- **消息区外框移除**：独立 AI 对话页取消包住消息列表的外层大框，聊天内容直接铺在白底工作区中，底部命令框输入器保持边界。
- **回答时间与输入框收紧**：AI 回答底部时间改为紧跟复制按钮显示，独立 AI 对话输入框高度进一步降低。
- **生图双击放大**：独立 AI 对话页生成后的图片可双击打开轻量预览层，支持遮罩、关闭按钮和 `Escape` 退出。
- **顶部信息栏移除**：删除 `AI 工作台 / 对话标题 / 消息数 / 本地对话` 这组页头文案，把更多高度还给实际对话区域。
- **对话区放大**：主消息列加宽，页内壳体改为更大的双段布局，减少上方空耗空间，让聊天区更接近完整页面。
- **滚动链路修复**：AI 页不再依赖全局 `overflow: hidden` 抑制滚动；长对话由 `.ai-chat-body` 自身负责滚动，发送后自动滚底恢复正常。

### 日志页与编辑器内 AI
- **日志归档页收口**：日志页保留顶部搜索与筛选工具区，日志卡片区从外层大白框中拆出，阅读区更轻。
- **筛选与分页整合**：分类、月份、页码统一为同一套下拉控件，页码切换移入筛选条，不再使用底部分页按钮组。
- **编辑器内 AI 工具区紧凑化**：日志内 AI 面板头部、历史入口、新对话和输入区动作收成更轻的工作带，减少重复说明和块状噪音。

### 文档与测试
- **README 同步当前界面**：补充最近 UI 变化，修正文档中对编辑器与 AI 页状态的描述。
- **回归测试更新**：同步 AI 对话页的 Codex 工作台布局、滚动和 DOM/CSS 断言，避免旧样式基线把问题重新锁回来。

---

## 2026-06-16

### 待办提醒邮件
- **每日到期提醒**：新增待办邮件提醒设置，支持在待办页面保存启用状态、收件邮箱和发送时间，默认每天 `08:00` 检查一次当天到期且未完成的待办。
- **香港业务日调度**：提醒调度沿用项目现有香港业务日期；若服务错过当天触发时间，会在恢复运行后的首次检查补发一次。
- **当天快照去重**：到点后会固定当天提醒快照；若 SMTP 失败，后续只重试同一份快照，不会把稍后新增或改动的待办混进当天邮件。
- **状态持久化**：新增提醒设置和提醒状态文件，服务重启后仍能保留启用状态、最近发送结果和当天去重信息。

### 邮件编码与手动测试
- **UTF-8 纯文本邮件**：待办提醒邮件改为更保守的 UTF-8 纯文本发送路径，显式使用文本编码，降低中文内容在收件端乱码的概率。
- **正文简化**：提醒邮件正文保留分类前缀、标题、截止日期和备注，继续移除优先级等更容易引起兼容性问题的字段。
- **手动测试脚本**：新增 `npm run todo:reminder:test`，可发送测试提醒邮件，或用 `--dry-run` 仅预览正文。

### 前端与测试
- **待办页提醒卡片**：待办页面右侧表单区新增“邮件提醒”设置卡片，并修复启用开关与区域滚动交互问题。
- **回归测试补齐**：增加提醒设置持久化、调度补发、失败重试、邮件正文构造和手动 dry-run 预览的测试覆盖。

---

## 2026-06-15

### 日志与日记
- **日记锁定空状态优化**：筛选到“日记”或其子分类且日记未解锁时，日志列表中间会显示“解锁日记”入口，不再误显示为空分类或旧日记查找。
- **解锁后保留筛选**：从日志列表中间解锁日记后，会保留当前日记分类、月份和搜索筛选并刷新结果。
- **AI 日志内链**：独立 AI 对话在允许访问日志时可返回 `[日志标题](#log/id)`，前端点击后会打开对应日志。

### AI 设置与对话
- **访问设置独立分区**：AI 设置新增“访问设置”，用于控制是否允许 AI 读取普通日志、日记，以及允许读取的父分类和子分类。
- **Tavily 快捷开关**：独立 AI 对话输入框左下角新增 Tavily 联网搜索开关，可快速开启或关闭自动搜索。
- **对话界面简化**：AI 对话主界面改为白底阅读区，用户问题使用浅蓝气泡，输入框保留单层灰边。
- **发送与生图按钮重排**：独立 AI 对话输入区改为圆形向上箭头发送按钮和独立圆形生图按钮，移除右下角全局 AI 浮动入口。
- **日志内 AI 提示词调整**：日志内 AI system prompt 从“只能使用当前编辑器上下文”改为“优先使用当前编辑器上下文”，回答空间更灵活。

### 待办与侧栏
- **待办按截止日期排序**：待办主页面的“待办”列表只显示有截止日期的未完成任务，并按截止日期升序排列。
- **无日期筛选**：原“全部”标签改为“无日期”，用于查看没有截止日期的未完成待办。
- **移除左侧待办小面板**：进入待办页面时左侧继续保留默认日志侧栏，不再显示重复的待办侧栏列表。

### 分类管理
- **分类图谱星系化**：父分类图谱视图改为更精致的星系式布局，节点表面只显示名称，日志数量移到可访问标签和悬浮提示。
- **分类浏览回退修复**：从分类管理进入子分类日志后，点击左侧父分类会回到对应父分类详情。
- **分类日志打开编辑器同步侧栏**：从分类管理的子分类日志列表打开日志编辑器时，侧栏会切回默认日志侧栏。

### 测试
- 增加 AI 访问设置、日志内链、待办截止日期排序、分类图谱、分类管理导航、日记锁定空状态和日志内 AI prompt 语义的回归测试。

---

## 2026-06-13

### 默认侧栏与日历
- **默认侧栏改为日志导航**：默认模式移除待办小面板和统计面板，改为日历、当前日志导航、日记锁与备份入口。
- **日历可收起**：日历折叠状态会记住；收起后只显示日期和星期几。
- **工具联动收放**：解锁日记、备份导出和导入恢复跟随日历展开/收起；更多工具模式仍可访问统计和数据工具。
- **日志导航放大**：当前日志导航成为默认侧栏主区域，折叠日历后自然占据更多高度。

### 分类管理
- **子分类拖拽排序**：分类管理页支持拖动当前父分类下的子分类；排序保存到 `categories.json` 的 `sub` 顺序。
- **筛选顺序同步**：日志筛选的二级分类下拉按保存后的子分类顺序展示。
- **图谱视图**：父分类详情新增“列表 / 图谱”切换，图谱展示当前父分类与子分类关系，点击子分类可进入现有浏览流程。
- **图谱偏好记忆**：分类详情视图模式保存到本地，下次打开任意父分类时沿用。

### AI 设置与技能
- **AI 设置独立页面**：AI 设置从弹窗迁移为应用内独立页面，左侧分区导航，右侧显示基础设置、生图设置和技能设置。
- **技能配置重组**：Tavily、Perplexity 和 WeStock 统一放入技能设置；Tavily/Perplexity 卡片可在标题区域内联展开配置。
- **Perplexity 自动联网搜索**：Perplexity 从手动确认工具卡改为和 Tavily 一样的自动联网搜索源。
- **多搜索源合并**：Tavily 和 Perplexity 可同时开启，发送消息时并行搜索并合并来源；单个搜索源失败不会阻断 AI 回复。
- **手动技能单选保留**：手动技能选择器只展示 WeStock 等真实工具技能，仍保持一次选择一个技能。
- **来源标注**：AI 回复来源会标明 `tavily` 或 `perplexity`，便于区分搜索结果。

### 待办与编辑体验
- **待办备注框增高**：待办主页面备注输入框最小高度从 66px 调整为 200px。
- **日志编辑 IME 修复跟进**：继续保留中文输入法组合输入保护，避免标点和数字被快捷键或编辑器事件吞掉。

### 测试
- 增加子分类排序、分类图谱、默认侧栏日历折叠、AI 设置页面、Perplexity 自动搜索、多搜索源合并和技能配置内联收放的回归测试。

---

## 2026-06-11

### 编辑器体验
- **中文输入法兼容**：日志正文编辑默认回退到原生 `textarea`，避免 CodeMirror/contenteditable 在中文输入法下吞掉标点和数字。
- **IME 快捷键保护**：输入法组合输入期间忽略全局快捷键和 `Process`/`229` 键盘事件，避免中文标点、数字被误判为命令。

### 测试
- 增加正文编辑器 IME composition、快捷键 IME 事件和默认 textarea 编辑路径的回归测试。

---

## 2026-06-06

### 待办事项
- **独立待办主界面**：侧边栏下拉选择“待办面板”后，右侧主体切换为完整待办工作区，不再停留在日志列表。
- **待办工作区重排**：新增待办、今日、逾期、已完成统计卡片，并保留待办 / 全部 / 已完成筛选。
- **待办搜索**：主界面新增搜索框，可按任务标题和备注过滤当前列表，搜索仅影响前端展示。
- **侧边栏轻量化**：待办模式下侧边栏只保留快速新增、待办数量、今日/逾期概览和紧凑待办列表，完整编辑表单移动到主界面。
- **文案修正**：侧边栏下拉菜单中的“代办面板”修正为“待办面板”。

### AI 对话与生图
- **AI 设置整合**：AI 设置弹窗新增“AI 设置 / 生图设置”双标签，统一管理 DeepSeek、Tavily、Seedream、流式输出和思考强度。
- **本地 AI 设置与历史**：AI 设置、API Key、全局对话历史和日志内对话历史保存到本机数据目录，不再依赖浏览器 `localStorage` 作为主要存储。
- **流式输出**：独立 AI 对话支持流式回复；开启后前端会增量渲染 Markdown 内容，并在完成后保存完整回复。
- **日志内 AI 面板**：编辑页新增右侧 AI 面板，可读取当前日志标题、正文和选区；AI 只生成回答和可应用建议，用户点击后才写入标题或正文。
- **日志内历史与设置**：日志内 AI 支持当前日志范围内的新对话、历史切换、重命名、删除，并复用全局 AI 设置弹窗。
- **AI 回复可读性**：日志内 AI 回复拆分为完整 Markdown 回答和可应用建议；标题、插入、替换全文建议提供可展开预览。
- **Seedream 生图**：新增 `/api/ai/image/generate`，支持单张图片生成并下载到本地 `uploads/`，独立 AI 和日志内 AI 均可展示图片预览与 Markdown 链接。
- **Prompt 优化确认**：点击生图会先调用 DeepSeek 优化 prompt，再展示原始 prompt 与优化 prompt 的确认卡片，由用户选择后生成图片。
- **显式生图入口**：独立 AI 和日志内 AI 输入框新增“生图”按钮；普通发送不再通过关键词自动识别生图意图。

### 文档与测试
- **README 重整**：README 改为快速开始、功能模块、AI、配置、数据隐私、备份、移动访问、模板和 API 的结构，补齐当前 AI 与待办主界面说明。
- **测试覆盖**：增加待办主界面、侧边栏待办概览、AI prompt 优化、Seedream 生图、日志内 AI、流式输出、本地 AI 设置和历史的回归测试。

---

## 2026-06-05

### AI 联网搜索
- **Tavily 接入**：AI 设置新增 Tavily API Key、联网搜索开关和搜索深度；开启后会用用户本轮输入的问题进行网页搜索，再交给 DeepSeek 生成回答。
- **来源展示**：AI 回复支持在内容下方展示 Tavily 来源链接，普通请求和流式输出都能携带来源。
- **隐私边界**：Tavily 只接收用户主动发送的最后一条消息，不读取日志、待办、分类或编辑器内容。

## 2026-06-04

### AI 对话助手
- **浮动入口改造**：右下角浮动按钮从“快速记录”改为“AI 对话”，点击进入独立 AI 对话页面，不再自动新建日志。
- **DeepSeek 对话接入**：新增 `/api/ai/chat` 后端代理接口，使用 DeepSeek Chat Completions API；前端不会直接暴露服务端环境变量中的密钥。
- **API Key 弹窗设置**：AI 页面新增 `API Key` 设置弹窗，用户可自行填写、保存或清除 DeepSeek API Key；该 Key 仅保存在当前浏览器 `localStorage`，不写入服务端数据文件。
- **模型与思考强度**：支持在前端切换 `deepseek-v4-flash` / `deepseek-v4-pro`，保留思考强度选择；思考模式固定开启，不再提供关闭按钮。
- **历史记录浮窗**：新增 `历史记录` 按钮，点击弹出历史浮窗；支持切换、新建、重命名、删除对话。
- **本地会话持久化**：AI 对话历史、当前选中对话和 API Key 都保存在浏览器本地 `localStorage`；刷新页面后仍可继续历史对话。
- **隐私边界**：AI 第一版不会读取日志、待办、分类或编辑器内容，只会发送用户在 AI 对话框中主动输入的消息。

### 配置与错误诊断
- **DeepSeek 环境变量**：`.env.example` 新增 `DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`、`DEEPSEEK_DEFAULT_MODEL`；服务端配置可作为默认 Key，前端本地 Key 优先。
- **安全错误提示**：DeepSeek 上游失败时返回带状态码的安全错误信息，帮助区分 Key、模型、权限或参数问题，同时过滤 `sk-...` 等敏感凭据。

### 测试
- 增加 AI 对话接口配置校验、参数白名单、上游错误清洗、本地历史 UI、API Key 弹窗、本地存储与固定思考开启的测试覆盖。

---

## 2026-06-02

### 编辑器体验
- **Markdown 标题大纲**：编辑页在“编辑 / 预览 / 分屏”左侧新增“标题”入口，可展开左侧标题大纲，自动解析当前日志正文中的 `#` 至 `######` 标题。
- **标题快速跳转**：点击大纲项会定位到对应 Markdown 标题行；若当前处于纯预览模式，会先切回编辑模式再跳转。
- **代码块识别**：标题大纲解析会跳过 fenced code block 内部内容，避免把代码片段中的 `#` 误识别为标题。
- **层级视觉增强**：大纲项新增 `H1`/`H2` 等层级徽标、彩色导线、不同层级字重与 hover/focus 动效，不再只依赖缩进区分层级。
- **全屏兼容**：专注全屏编辑模式保留标题大纲入口与展开面板，展开/收起后会刷新编辑器布局，避免内容区域尺寸错位。

### 分类管理
- **分类日志计数**：分类 API 返回父分类日志数量与各子分类日志数量，管理页可直接显示计数徽标。
- **管理页信息密度优化**：父分类列表改为更紧凑的拖拽排序视图，移除上下移动按钮；父分类数量摘要改为小型徽标。
- **子分类浏览**：子分类可进入独立浏览视图，展示该子分类下的日志列表，并可直接打开对应日志。
- **操作区重排**：新增父分类入口移动到页面标题区域；子分类添加、重命名、删除改为更紧凑的图标按钮，并补齐可访问名称。
- **日志数量徽标**：父分类详情、子分类列表和子分类浏览页统一使用日志数量徽标，减少需要点开后猜内容的情况。

### 待办事项
- **优先级体系调整**：待办优先级从旧的 `low / normal / high` 调整为 `none / normal / important / urgent`，并兼容旧值导入映射。
- **优先级展示升级**：优先级从小圆点改为 `P2`、`P1`、`P0` 样式徽标，待办列表可读性更强。
- **拖拽排序简化**：待办排序保留拖拽交互，移除上下移动按钮。
- **备注显示收敛**：待办列表不再展示备注预览，降低侧边栏视觉噪音。
- **恢复校验兼容**：备份恢复校验支持新的待办优先级，同时继续接受旧优先级值并规范化。

### 测试
- 增加分类日志计数、分类管理 UI、子分类浏览、待办新优先级、标题大纲结构、标题大纲跳转逻辑和全屏兼容性的测试覆盖。

---

## 2026-05-27

### 编辑器体验
- **CodeMirror 编辑器迁移**：日志 Markdown 编辑底层由 Monaco 替换为 CodeMirror，桌面与手机统一使用语法高亮编辑表面，并支持主题联动。
- **编辑器进入页面修复**：补齐 CodeMirror 布局刷新接口，修复迁移后进入日志编辑页面可能出现 `contentEditor.layout is not a function` 的错误。
- **图片直接粘贴上传**：在正文编辑区域粘贴剪贴板图片即可上传，并在原光标位置插入 Markdown 图片链接；修复 Monaco 编辑器拦截粘贴事件导致上传失效的问题。
- **常用 Emoji 工具栏**：新增 `✅`、`📌`、`💡`、`⚠️`、`🚀` 快速插入按钮。
- **专注全屏编辑**：编辑页新增全屏切换入口；全屏中保留返回、退出全屏、编辑/预览/分屏切换、Markdown 快捷工具栏及内容区域，隐藏元信息、模板和管理操作；按 `Esc` 优先退出全屏。
- **分类筛选自适应宽度**：列表筛选与编辑器分类选择框会依据选项内容调整宽度，聚焦样式保持蓝色强调。
- **手机顶部区域重整**：手机首屏改为紧凑操作栏，日历、日志导航、待办和统计/备份按需展开；同时提供整体收起与浮动恢复按钮。
- **手机编辑与筛选优化**：筛选项改为适配窄屏的栅格排列，编辑元信息双列展示，Markdown 工具栏可横向滑动并扩大触控区域。

### 分类与日历
- **日历按日可见性设置**：管理分类新增“日历显示”开关，可控制父分类及其子分类是否出现在点击具体日期后的日志列表中。
- **月份筛选保持完整**：关闭日历显示不会隐藏按月份查询所得日志，也不会删除或修改原日志内容。
- **管理页面布局优化**：分类搜索框移至页面标题右侧；日历显示设置改为位于“重命名”前的紧凑开关，并提供悬浮说明。
- **数据兼容与备份恢复**：分类数据增加 `calendar_day_visible` 字段，旧数据自动默认可见，备份恢复包含校验与持久化支持。
- **新建日志上下文预填**：从日历选中某一天后新建日志会沿用该日期，未选中时使用当天日期；存在父分类或二级分类筛选时，新日志会继承当前筛选分类。

### 模板格式化
- **中文日期占位符**：模板支持 `{{今天}}`、`{{昨天}}`、`{{明天}}`、`{{日期:+7:MM月DD日}}` 等表达式，同时兼容既有英文占位符。
- **周区间占位符**：新增 `{{本周:MM月DD日}}`、`{{上一周:MM月DD日}}`、`{{下一周:MM月DD日}}` 及 `.开始`、`.结束` 边界格式。
- **周复盘模板升级**：默认周复盘模板直接生成本周、上周及下周区间标题。

### 测试
- 增加图片粘贴与 Emoji 控件、模板区间格式化、分类日历显示接口、手机侧边栏收起入口、CodeMirror 布局兼容、专注全屏预览切换、新建日志默认值与备份恢复兼容性测试。

---

## 2026-05-10

### 卡片 UI 重构
- **横向滑动布局**：卡片从纵向堆叠改为横向排列，支持左右滑动浏览，桌面端每张卡片默认 300px 宽
- **卡片宽度可拖拽调整**：hover 卡片右侧出现拖拽把手，可调整 200-800px 范围宽度，工具栏 🔲 按钮一键恢复默认
- **竖版卡片结构**：每张卡片内部自上而下为「标题行 → 正文预览 → 日期戳」，信息层级清晰
- **预览区独立滚动**：正文超长时可在卡片内部滑动查看，不再截断内容

### 预览增强
- **Markdown / 纯文本切换**：每张卡片顶部新增 ◧ 按钮，一键在 Markdown 渲染和纯文本之间切换
- **预览区撑满卡片**：正文预览自动填充卡片可用高度，充分利用展示空间

### 编辑体验
- **默认标题**：创建日志时标题为空则自动使用"未命名日志"
- **默认分类**：新建日志默认分类设为"其他"

### 分类管理
- **"其他"分类锁定**：系统默认分类"其他"不可删除，前端隐藏删除按钮，后端已有保护逻辑

### 功能移除
- **删除置顶功能**：简化卡片交互，移除 pin/置顶相关的前后端代码

### 缺陷修复
- 修复从编辑器返回列表后卡片高度塌陷的问题（`display: block` → `flex`）
- 修复卡片预览顶部空白问题（`white-space: pre-wrap` 作用范围修正）
- 修复置顶功能完全无效的 bug（后端路由漏传 `pinned` 字段）

---

## 未来优化方向

### 数据与同步
- **移动端访问**：通过 ngrok 等内网穿透工具实现手机远程访问，或部署到云服务器
- **数据库升级**：从 JSON 文件存储迁移到 SQLite，提升并发读写和查询性能
- **数据导出增强**：支持按日期范围、分类组合筛选导出，增加 JSON 格式导出

### 编辑器
- **富文本编辑**：在 Markdown 基础上增加所见即所得工具栏（加粗、标题、列表等快捷按钮）
- **标签系统**：为日志增加多标签支持，替代单一分类

### 卡片视图
- **卡片密度切换**：提供紧凑/舒适两种卡片密度模式
- **排序选项**：支持按日期、工时、分类等维度排序
- **筛选增强**：增加工时范围筛选、日期区间筛选

### 统计与分析
- **工时趋势图**：可视化每日/每周工时变化
- **分类统计饼图**：直观展示各分类占比
- **词云分析**：对日志内容进行关键词提取和词云展示

### 体验优化
- **键盘快捷键扩展**：更多全局快捷键，减少鼠标操作
- **暗色模式优化**：根据系统自动切换，细节色彩调整
- **PWA 支持**：支持离线访问、桌面快捷方式
